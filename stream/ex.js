// @ts-check
/**
 * @typedef {Object} Issue
 * @param {number} a
 * @param {number} b
 * @return {number}
 * 
 */

function t(a, b) {
    return a + b
}
let d = t(1, 3)

let express = require('express')
let app = express()

/**@type { {a: string} } */
let issue = {a:'k'}
 

/** @type { number[] } */

let bem = [0]

